#include"1.h"
BSTNode BSTBuyNode(BSTDataType x)
{
	BSTNode newnode = (BSTNode)malloc(sizeof(BSTreeNode));
	if (NULL == newnode)
		return;
}
int BSTreeInsert(BSTNode *ppTree, BSTDataType x)//≤Â»Î
{
	BSTNode cur, parent;
	parent = NULL;
	if (ppTree == NULL);
	{
		ppTree = BSTBuyNode(x);
		return 1;
	}

}